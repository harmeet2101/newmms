package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpense;

import com.mbopartners.mbomobile.data.db.generated.model.TableReceipt;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_RECEIPT".
*/
public class TableReceiptDao extends AbstractDao<TableReceipt, Long> {

    public static final String TABLENAME = "TABLE_RECEIPT";

    /**
     * Properties of entity TableReceipt.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property RecState = new Property(1, int.class, "RecState", false, "REC_STATE");
        public final static Property Filename = new Property(2, String.class, "Filename", false, "FILENAME");
        public final static Property MboExpenseId = new Property(3, String.class, "MboExpenseId", false, "MBO_EXPENSE_ID");
        public final static Property CreationDate = new Property(4, java.util.Date.class, "CreationDate", false, "CREATION_DATE");
        public final static Property ThumbnailPath = new Property(5, String.class, "ThumbnailPath", false, "THUMBNAIL_PATH");
        public final static Property Version = new Property(6, Double.class, "Version", false, "VERSION");
        public final static Property ExpenseId = new Property(7, long.class, "ExpenseId", false, "EXPENSE_ID");
    };

    private DaoSession daoSession;

    private Query<TableReceipt> tableExpense_ReceiptsQuery;

    public TableReceiptDao(DaoConfig config) {
        super(config);
    }
    
    public TableReceiptDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_RECEIPT\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"REC_STATE\" INTEGER NOT NULL ," + // 1: RecState
                "\"FILENAME\" TEXT NOT NULL ," + // 2: Filename
                "\"MBO_EXPENSE_ID\" TEXT NOT NULL ," + // 3: MboExpenseId
                "\"CREATION_DATE\" INTEGER NOT NULL ," + // 4: CreationDate
                "\"THUMBNAIL_PATH\" TEXT," + // 5: ThumbnailPath
                "\"VERSION\" REAL," + // 6: Version
                "\"EXPENSE_ID\" INTEGER NOT NULL );"); // 7: ExpenseId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_RECEIPT\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableReceipt entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getRecState());
        stmt.bindString(3, entity.getFilename());
        stmt.bindString(4, entity.getMboExpenseId());
        stmt.bindLong(5, entity.getCreationDate().getTime());
 
        String ThumbnailPath = entity.getThumbnailPath();
        if (ThumbnailPath != null) {
            stmt.bindString(6, ThumbnailPath);
        }
 
        Double Version = entity.getVersion();
        if (Version != null) {
            stmt.bindDouble(7, Version);
        }
        stmt.bindLong(8, entity.getExpenseId());
    }

    @Override
    protected void attachEntity(TableReceipt entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableReceipt readEntity(Cursor cursor, int offset) {
        TableReceipt entity = new TableReceipt( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // RecState
            cursor.getString(offset + 2), // Filename
            cursor.getString(offset + 3), // MboExpenseId
            new java.util.Date(cursor.getLong(offset + 4)), // CreationDate
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // ThumbnailPath
            cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6), // Version
            cursor.getLong(offset + 7) // ExpenseId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableReceipt entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setRecState(cursor.getInt(offset + 1));
        entity.setFilename(cursor.getString(offset + 2));
        entity.setMboExpenseId(cursor.getString(offset + 3));
        entity.setCreationDate(new java.util.Date(cursor.getLong(offset + 4)));
        entity.setThumbnailPath(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setVersion(cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6));
        entity.setExpenseId(cursor.getLong(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableReceipt entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableReceipt entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "Receipts" to-many relationship of TableExpense. */
    public List<TableReceipt> _queryTableExpense_Receipts(long ExpenseId) {
        synchronized (this) {
            if (tableExpense_ReceiptsQuery == null) {
                QueryBuilder<TableReceipt> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ExpenseId.eq(null));
                tableExpense_ReceiptsQuery = queryBuilder.build();
            }
        }
        Query<TableReceipt> query = tableExpense_ReceiptsQuery.forCurrentThread();
        query.setParameter(0, ExpenseId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableExpenseDao().getAllColumns());
            builder.append(" FROM TABLE_RECEIPT T");
            builder.append(" LEFT JOIN TABLE_EXPENSE T0 ON T.\"EXPENSE_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableReceipt loadCurrentDeep(Cursor cursor, boolean lock) {
        TableReceipt entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableExpense tableExpense = loadCurrentOther(daoSession.getTableExpenseDao(), cursor, offset);
         if(tableExpense != null) {
            entity.setTableExpense(tableExpense);
        }

        return entity;    
    }

    public TableReceipt loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableReceipt> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableReceipt> list = new ArrayList<TableReceipt>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableReceipt> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableReceipt> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
