package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mbopartners.mbomobile.data.db.generated.model.TableWorkOrder;

import com.mbopartners.mbomobile.data.db.generated.model.TableTimeTask;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_TIME_TASK".
*/
public class TableTimeTaskDao extends AbstractDao<TableTimeTask, Long> {

    public static final String TABLENAME = "TABLE_TIME_TASK";

    /**
     * Properties of entity TableTimeTask.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MboId = new Property(1, String.class, "MboId", false, "MBO_ID");
        public final static Property Name = new Property(2, String.class, "Name", false, "NAME");
        public final static Property WorkOrderId = new Property(3, long.class, "WorkOrderId", false, "WORK_ORDER_ID");
    };

    private DaoSession daoSession;

    private Query<TableTimeTask> tableWorkOrder_TimeTasksQuery;

    public TableTimeTaskDao(DaoConfig config) {
        super(config);
    }
    
    public TableTimeTaskDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_TIME_TASK\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MBO_ID\" TEXT NOT NULL ," + // 1: MboId
                "\"NAME\" TEXT NOT NULL ," + // 2: Name
                "\"WORK_ORDER_ID\" INTEGER NOT NULL );"); // 3: WorkOrderId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_TIME_TASK\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableTimeTask entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMboId());
        stmt.bindString(3, entity.getName());
        stmt.bindLong(4, entity.getWorkOrderId());
    }

    @Override
    protected void attachEntity(TableTimeTask entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableTimeTask readEntity(Cursor cursor, int offset) {
        TableTimeTask entity = new TableTimeTask( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // MboId
            cursor.getString(offset + 2), // Name
            cursor.getLong(offset + 3) // WorkOrderId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableTimeTask entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMboId(cursor.getString(offset + 1));
        entity.setName(cursor.getString(offset + 2));
        entity.setWorkOrderId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableTimeTask entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableTimeTask entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "TimeTasks" to-many relationship of TableWorkOrder. */
    public List<TableTimeTask> _queryTableWorkOrder_TimeTasks(long WorkOrderId) {
        synchronized (this) {
            if (tableWorkOrder_TimeTasksQuery == null) {
                QueryBuilder<TableTimeTask> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.WorkOrderId.eq(null));
                tableWorkOrder_TimeTasksQuery = queryBuilder.build();
            }
        }
        Query<TableTimeTask> query = tableWorkOrder_TimeTasksQuery.forCurrentThread();
        query.setParameter(0, WorkOrderId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableWorkOrderDao().getAllColumns());
            builder.append(" FROM TABLE_TIME_TASK T");
            builder.append(" LEFT JOIN TABLE_WORK_ORDER T0 ON T.\"WORK_ORDER_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableTimeTask loadCurrentDeep(Cursor cursor, boolean lock) {
        TableTimeTask entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableWorkOrder tableWorkOrder = loadCurrentOther(daoSession.getTableWorkOrderDao(), cursor, offset);
         if(tableWorkOrder != null) {
            entity.setTableWorkOrder(tableWorkOrder);
        }

        return entity;    
    }

    public TableTimeTask loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableTimeTask> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableTimeTask> list = new ArrayList<TableTimeTask>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableTimeTask> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableTimeTask> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
