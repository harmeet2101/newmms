package com.mbopartners.mbomobile.data.db.generated.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.mbopartners.mbomobile.data.db.generated.model.TableBusinessManager;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_BUSINESS_MANAGER".
*/
public class TableBusinessManagerDao extends AbstractDao<TableBusinessManager, Long> {

    public static final String TABLENAME = "TABLE_BUSINESS_MANAGER";

    /**
     * Properties of entity TableBusinessManager.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MboId = new Property(1, String.class, "MboId", false, "MBO_ID");
        public final static Property FirstName = new Property(2, String.class, "FirstName", false, "FIRST_NAME");
        public final static Property LastName = new Property(3, String.class, "LastName", false, "LAST_NAME");
        public final static Property Email = new Property(4, String.class, "Email", false, "EMAIL");
    };


    public TableBusinessManagerDao(DaoConfig config) {
        super(config);
    }
    
    public TableBusinessManagerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_BUSINESS_MANAGER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MBO_ID\" TEXT NOT NULL ," + // 1: MboId
                "\"FIRST_NAME\" TEXT NOT NULL ," + // 2: FirstName
                "\"LAST_NAME\" TEXT NOT NULL ," + // 3: LastName
                "\"EMAIL\" TEXT NOT NULL );"); // 4: Email
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_BUSINESS_MANAGER\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableBusinessManager entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMboId());
        stmt.bindString(3, entity.getFirstName());
        stmt.bindString(4, entity.getLastName());
        stmt.bindString(5, entity.getEmail());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableBusinessManager readEntity(Cursor cursor, int offset) {
        TableBusinessManager entity = new TableBusinessManager( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // MboId
            cursor.getString(offset + 2), // FirstName
            cursor.getString(offset + 3), // LastName
            cursor.getString(offset + 4) // Email
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableBusinessManager entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMboId(cursor.getString(offset + 1));
        entity.setFirstName(cursor.getString(offset + 2));
        entity.setLastName(cursor.getString(offset + 3));
        entity.setEmail(cursor.getString(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableBusinessManager entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableBusinessManager entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
