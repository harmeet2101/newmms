package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mbopartners.mbomobile.data.db.generated.model.TableDashboard;

import com.mbopartners.mbomobile.data.db.generated.model.TableDashboardField;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_DASHBOARD_FIELD".
*/
public class TableDashboardFieldDao extends AbstractDao<TableDashboardField, Long> {

    public static final String TABLENAME = "TABLE_DASHBOARD_FIELD";

    /**
     * Properties of entity TableDashboardField.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "Name", false, "NAME");
        public final static Property Value = new Property(2, String.class, "Value", false, "VALUE");
        public final static Property DashboardId = new Property(3, long.class, "DashboardId", false, "DASHBOARD_ID");
    };

    private DaoSession daoSession;

    private Query<TableDashboardField> tableDashboard_FieldsQuery;

    public TableDashboardFieldDao(DaoConfig config) {
        super(config);
    }
    
    public TableDashboardFieldDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_DASHBOARD_FIELD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: Name
                "\"VALUE\" TEXT," + // 2: Value
                "\"DASHBOARD_ID\" INTEGER NOT NULL );"); // 3: DashboardId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_DASHBOARD_FIELD\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableDashboardField entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String Name = entity.getName();
        if (Name != null) {
            stmt.bindString(2, Name);
        }
 
        String Value = entity.getValue();
        if (Value != null) {
            stmt.bindString(3, Value);
        }
        stmt.bindLong(4, entity.getDashboardId());
    }

    @Override
    protected void attachEntity(TableDashboardField entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableDashboardField readEntity(Cursor cursor, int offset) {
        TableDashboardField entity = new TableDashboardField( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // Name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // Value
            cursor.getLong(offset + 3) // DashboardId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableDashboardField entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setValue(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDashboardId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableDashboardField entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableDashboardField entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "Fields" to-many relationship of TableDashboard. */
    public List<TableDashboardField> _queryTableDashboard_Fields(long DashboardId) {
        synchronized (this) {
            if (tableDashboard_FieldsQuery == null) {
                QueryBuilder<TableDashboardField> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.DashboardId.eq(null));
                tableDashboard_FieldsQuery = queryBuilder.build();
            }
        }
        Query<TableDashboardField> query = tableDashboard_FieldsQuery.forCurrentThread();
        query.setParameter(0, DashboardId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableDashboardDao().getAllColumns());
            builder.append(" FROM TABLE_DASHBOARD_FIELD T");
            builder.append(" LEFT JOIN TABLE_DASHBOARD T0 ON T.\"DASHBOARD_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableDashboardField loadCurrentDeep(Cursor cursor, boolean lock) {
        TableDashboardField entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableDashboard tableDashboard = loadCurrentOther(daoSession.getTableDashboardDao(), cursor, offset);
         if(tableDashboard != null) {
            entity.setTableDashboard(tableDashboard);
        }

        return entity;    
    }

    public TableDashboardField loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableDashboardField> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableDashboardField> list = new ArrayList<TableDashboardField>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableDashboardField> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableDashboardField> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
