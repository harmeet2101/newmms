package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpenseField;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpenseFieldValue;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_EXPENSE_FIELD_VALUE".
*/
public class TableExpenseFieldValueDao extends AbstractDao<TableExpenseFieldValue, Long> {

    public static final String TABLENAME = "TABLE_EXPENSE_FIELD_VALUE";

    /**
     * Properties of entity TableExpenseFieldValue.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MboId = new Property(1, String.class, "MboId", false, "MBO_ID");
        public final static Property Value = new Property(2, String.class, "Value", false, "VALUE");
        public final static Property ExpenseFieldId = new Property(3, long.class, "ExpenseFieldId", false, "EXPENSE_FIELD_ID");
    };

    private DaoSession daoSession;

    private Query<TableExpenseFieldValue> tableExpenseField_ValuesQuery;

    public TableExpenseFieldValueDao(DaoConfig config) {
        super(config);
    }
    
    public TableExpenseFieldValueDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_EXPENSE_FIELD_VALUE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MBO_ID\" TEXT," + // 1: MboId
                "\"VALUE\" TEXT," + // 2: Value
                "\"EXPENSE_FIELD_ID\" INTEGER NOT NULL );"); // 3: ExpenseFieldId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_EXPENSE_FIELD_VALUE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableExpenseFieldValue entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String MboId = entity.getMboId();
        if (MboId != null) {
            stmt.bindString(2, MboId);
        }
 
        String Value = entity.getValue();
        if (Value != null) {
            stmt.bindString(3, Value);
        }
        stmt.bindLong(4, entity.getExpenseFieldId());
    }

    @Override
    protected void attachEntity(TableExpenseFieldValue entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableExpenseFieldValue readEntity(Cursor cursor, int offset) {
        TableExpenseFieldValue entity = new TableExpenseFieldValue( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // MboId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // Value
            cursor.getLong(offset + 3) // ExpenseFieldId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableExpenseFieldValue entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMboId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setValue(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setExpenseFieldId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableExpenseFieldValue entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableExpenseFieldValue entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "Values" to-many relationship of TableExpenseField. */
    public List<TableExpenseFieldValue> _queryTableExpenseField_Values(long ExpenseFieldId) {
        synchronized (this) {
            if (tableExpenseField_ValuesQuery == null) {
                QueryBuilder<TableExpenseFieldValue> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ExpenseFieldId.eq(null));
                tableExpenseField_ValuesQuery = queryBuilder.build();
            }
        }
        Query<TableExpenseFieldValue> query = tableExpenseField_ValuesQuery.forCurrentThread();
        query.setParameter(0, ExpenseFieldId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableExpenseFieldDao().getAllColumns());
            builder.append(" FROM TABLE_EXPENSE_FIELD_VALUE T");
            builder.append(" LEFT JOIN TABLE_EXPENSE_FIELD T0 ON T.\"EXPENSE_FIELD_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableExpenseFieldValue loadCurrentDeep(Cursor cursor, boolean lock) {
        TableExpenseFieldValue entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableExpenseField tableExpenseField = loadCurrentOther(daoSession.getTableExpenseFieldDao(), cursor, offset);
         if(tableExpenseField != null) {
            entity.setTableExpenseField(tableExpenseField);
        }

        return entity;    
    }

    public TableExpenseFieldValue loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableExpenseFieldValue> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableExpenseFieldValue> list = new ArrayList<TableExpenseFieldValue>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableExpenseFieldValue> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableExpenseFieldValue> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
