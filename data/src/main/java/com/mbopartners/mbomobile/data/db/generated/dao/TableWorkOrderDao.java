package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.mbopartners.mbomobile.data.db.generated.model.TableCompany;

import com.mbopartners.mbomobile.data.db.generated.model.TableWorkOrder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_WORK_ORDER".
*/
public class TableWorkOrderDao extends AbstractDao<TableWorkOrder, Long> {

    public static final String TABLENAME = "TABLE_WORK_ORDER";

    /**
     * Properties of entity TableWorkOrder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MboId = new Property(1, String.class, "MboId", false, "MBO_ID");
        public final static Property Name = new Property(2, String.class, "Name", false, "NAME");
        public final static Property TimeEntryAllowed = new Property(3, boolean.class, "TimeEntryAllowed", false, "TIME_ENTRY_ALLOWED");
        public final static Property BillableExpensesAllowed = new Property(4, boolean.class, "BillableExpensesAllowed", false, "BILLABLE_EXPENSES_ALLOWED");
        public final static Property CompanyId = new Property(5, long.class, "CompanyId", false, "COMPANY_ID");
    };

    private DaoSession daoSession;


    public TableWorkOrderDao(DaoConfig config) {
        super(config);
    }
    
    public TableWorkOrderDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_WORK_ORDER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MBO_ID\" TEXT NOT NULL ," + // 1: MboId
                "\"NAME\" TEXT NOT NULL ," + // 2: Name
                "\"TIME_ENTRY_ALLOWED\" INTEGER NOT NULL ," + // 3: TimeEntryAllowed
                "\"BILLABLE_EXPENSES_ALLOWED\" INTEGER NOT NULL ," + // 4: BillableExpensesAllowed
                "\"COMPANY_ID\" INTEGER NOT NULL );"); // 5: CompanyId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_WORK_ORDER\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableWorkOrder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMboId());
        stmt.bindString(3, entity.getName());
        stmt.bindLong(4, entity.getTimeEntryAllowed() ? 1L: 0L);
        stmt.bindLong(5, entity.getBillableExpensesAllowed() ? 1L: 0L);
        stmt.bindLong(6, entity.getCompanyId());
    }

    @Override
    protected void attachEntity(TableWorkOrder entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableWorkOrder readEntity(Cursor cursor, int offset) {
        TableWorkOrder entity = new TableWorkOrder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // MboId
            cursor.getString(offset + 2), // Name
            cursor.getShort(offset + 3) != 0, // TimeEntryAllowed
            cursor.getShort(offset + 4) != 0, // BillableExpensesAllowed
            cursor.getLong(offset + 5) // CompanyId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableWorkOrder entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMboId(cursor.getString(offset + 1));
        entity.setName(cursor.getString(offset + 2));
        entity.setTimeEntryAllowed(cursor.getShort(offset + 3) != 0);
        entity.setBillableExpensesAllowed(cursor.getShort(offset + 4) != 0);
        entity.setCompanyId(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableWorkOrder entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableWorkOrder entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableCompanyDao().getAllColumns());
            builder.append(" FROM TABLE_WORK_ORDER T");
            builder.append(" LEFT JOIN TABLE_COMPANY T0 ON T.\"COMPANY_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableWorkOrder loadCurrentDeep(Cursor cursor, boolean lock) {
        TableWorkOrder entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableCompany tableCompany = loadCurrentOther(daoSession.getTableCompanyDao(), cursor, offset);
         if(tableCompany != null) {
            entity.setTableCompany(tableCompany);
        }

        return entity;    
    }

    public TableWorkOrder loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableWorkOrder> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableWorkOrder> list = new ArrayList<TableWorkOrder>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableWorkOrder> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableWorkOrder> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
