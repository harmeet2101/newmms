package com.mbopartners.mbomobile.data.db.generated.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpenseType;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_EXPENSE_TYPE".
*/
public class TableExpenseTypeDao extends AbstractDao<TableExpenseType, String> {

    public static final String TABLENAME = "TABLE_EXPENSE_TYPE";

    /**
     * Properties of entity TableExpenseType.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property MboId = new Property(0, String.class, "MboId", true, "MBO_ID");
        public final static Property Name = new Property(1, String.class, "Name", false, "NAME");
    };

    private DaoSession daoSession;


    public TableExpenseTypeDao(DaoConfig config) {
        super(config);
    }
    
    public TableExpenseTypeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_EXPENSE_TYPE\" (" + //
                "\"MBO_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: MboId
                "\"NAME\" TEXT NOT NULL );"); // 1: Name
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_EXPENSE_TYPE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableExpenseType entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getMboId());
        stmt.bindString(2, entity.getName());
    }

    @Override
    protected void attachEntity(TableExpenseType entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableExpenseType readEntity(Cursor cursor, int offset) {
        TableExpenseType entity = new TableExpenseType( //
            cursor.getString(offset + 0), // MboId
            cursor.getString(offset + 1) // Name
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableExpenseType entity, int offset) {
        entity.setMboId(cursor.getString(offset + 0));
        entity.setName(cursor.getString(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(TableExpenseType entity, long rowId) {
        return entity.getMboId();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(TableExpenseType entity) {
        if(entity != null) {
            return entity.getMboId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
