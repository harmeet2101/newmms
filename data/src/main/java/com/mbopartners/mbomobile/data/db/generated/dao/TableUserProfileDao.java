package com.mbopartners.mbomobile.data.db.generated.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.mbopartners.mbomobile.data.db.generated.model.TableUserProfile;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_USER_PROFILE".
*/
public class TableUserProfileDao extends AbstractDao<TableUserProfile, Long> {

    public static final String TABLENAME = "TABLE_USER_PROFILE";

    /**
     * Properties of entity TableUserProfile.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MboId = new Property(1, String.class, "MboId", false, "MBO_ID");
        public final static Property Number = new Property(2, String.class, "Number", false, "NUMBER");
        public final static Property Status = new Property(3, String.class, "Status", false, "STATUS");
        public final static Property FirstName = new Property(4, String.class, "FirstName", false, "FIRST_NAME");
        public final static Property LastName = new Property(5, String.class, "LastName", false, "LAST_NAME");
        public final static Property Email = new Property(6, String.class, "Email", false, "EMAIL");
        public final static Property NonbillableAllowed = new Property(7, Boolean.class, "NonbillableAllowed", false, "NONBILLABLE_ALLOWED");
    };


    public TableUserProfileDao(DaoConfig config) {
        super(config);
    }
    
    public TableUserProfileDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_USER_PROFILE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MBO_ID\" TEXT NOT NULL ," + // 1: MboId
                "\"NUMBER\" TEXT," + // 2: Number
                "\"STATUS\" TEXT," + // 3: Status
                "\"FIRST_NAME\" TEXT," + // 4: FirstName
                "\"LAST_NAME\" TEXT," + // 5: LastName
                "\"EMAIL\" TEXT," + // 6: Email
                "\"NONBILLABLE_ALLOWED\" INTEGER);"); // 7: NonbillableAllowed
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_USER_PROFILE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableUserProfile entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMboId());
 
        String Number = entity.getNumber();
        if (Number != null) {
            stmt.bindString(3, Number);
        }
 
        String Status = entity.getStatus();
        if (Status != null) {
            stmt.bindString(4, Status);
        }
 
        String FirstName = entity.getFirstName();
        if (FirstName != null) {
            stmt.bindString(5, FirstName);
        }
 
        String LastName = entity.getLastName();
        if (LastName != null) {
            stmt.bindString(6, LastName);
        }
 
        String Email = entity.getEmail();
        if (Email != null) {
            stmt.bindString(7, Email);
        }
 
        Boolean NonbillableAllowed = entity.getNonbillableAllowed();
        if (NonbillableAllowed != null) {
            stmt.bindLong(8, NonbillableAllowed ? 1L: 0L);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableUserProfile readEntity(Cursor cursor, int offset) {
        TableUserProfile entity = new TableUserProfile( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // MboId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // Number
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // Status
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // FirstName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // LastName
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // Email
            cursor.isNull(offset + 7) ? null : cursor.getShort(offset + 7) != 0 // NonbillableAllowed
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableUserProfile entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMboId(cursor.getString(offset + 1));
        entity.setNumber(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setStatus(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFirstName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setLastName(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setEmail(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setNonbillableAllowed(cursor.isNull(offset + 7) ? null : cursor.getShort(offset + 7) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableUserProfile entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableUserProfile entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
