package com.mbopartners.mbomobile.data.db.generated.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpense;

import com.mbopartners.mbomobile.data.db.generated.model.TableExpenseData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TABLE_EXPENSE_DATA".
*/
public class TableExpenseDataDao extends AbstractDao<TableExpenseData, Long> {

    public static final String TABLENAME = "TABLE_EXPENSE_DATA";

    /**
     * Properties of entity TableExpenseData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "Name", false, "NAME");
        public final static Property Value = new Property(2, String.class, "Value", false, "VALUE");
        public final static Property ExpenseId = new Property(3, long.class, "ExpenseId", false, "EXPENSE_ID");
    };

    private DaoSession daoSession;

    private Query<TableExpenseData> tableExpense_ExpenseDataQuery;

    public TableExpenseDataDao(DaoConfig config) {
        super(config);
    }
    
    public TableExpenseDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TABLE_EXPENSE_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT NOT NULL ," + // 1: Name
                "\"VALUE\" TEXT NOT NULL ," + // 2: Value
                "\"EXPENSE_ID\" INTEGER NOT NULL );"); // 3: ExpenseId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TABLE_EXPENSE_DATA\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TableExpenseData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getValue());
        stmt.bindLong(4, entity.getExpenseId());
    }

    @Override
    protected void attachEntity(TableExpenseData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TableExpenseData readEntity(Cursor cursor, int offset) {
        TableExpenseData entity = new TableExpenseData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // Name
            cursor.getString(offset + 2), // Value
            cursor.getLong(offset + 3) // ExpenseId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TableExpenseData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.getString(offset + 1));
        entity.setValue(cursor.getString(offset + 2));
        entity.setExpenseId(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TableExpenseData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TableExpenseData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "ExpenseData" to-many relationship of TableExpense. */
    public List<TableExpenseData> _queryTableExpense_ExpenseData(long ExpenseId) {
        synchronized (this) {
            if (tableExpense_ExpenseDataQuery == null) {
                QueryBuilder<TableExpenseData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ExpenseId.eq(null));
                tableExpense_ExpenseDataQuery = queryBuilder.build();
            }
        }
        Query<TableExpenseData> query = tableExpense_ExpenseDataQuery.forCurrentThread();
        query.setParameter(0, ExpenseId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTableExpenseDao().getAllColumns());
            builder.append(" FROM TABLE_EXPENSE_DATA T");
            builder.append(" LEFT JOIN TABLE_EXPENSE T0 ON T.\"EXPENSE_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TableExpenseData loadCurrentDeep(Cursor cursor, boolean lock) {
        TableExpenseData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TableExpense tableExpense = loadCurrentOther(daoSession.getTableExpenseDao(), cursor, offset);
         if(tableExpense != null) {
            entity.setTableExpense(tableExpense);
        }

        return entity;    
    }

    public TableExpenseData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TableExpenseData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TableExpenseData> list = new ArrayList<TableExpenseData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TableExpenseData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TableExpenseData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
